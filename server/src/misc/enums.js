const USER_ROLES = {
  default: 'default',
  employee: 'employee',
  admin: 'admin',
};

const APPEAL_STATUSES = {
  opened: 'opened',
  closed: 'closed',
};

module.exports = {
  USER_ROLES,
  APPEAL_STATUSES,
};
